from django import forms
from map.models import Markers
"""
class AddMarker(forms.ModelForm):
    ""Adds a marker to the map. Initially not visible until a moderator approves it.

    Args:
        forms (_type_): _description_
    ""
    class Meta:
        model = Markers
        fields = [] #TODO - addto
"""